{
  "name": "symphony-tui",
  "version": "0.1.0",
  "description": "OpenTUI operations cockpit for Symphony",
  "private": true,
  "type": "module",
  "module": "src/main.ts",
  "engines": {
    "bun": ">=1.1.0"
  },
  "scripts": {
    "start": "bun run src/index.ts",
    "cockpit": "bun run src/main.ts",
    "test": "bun test",
    "check": "bun x tsc --noEmit",
    "typecheck": "bun x tsc --noEmit"
  },
  "dependencies": {
    "@opentui/core": "0.2.2"
  },
  "devDependencies": {
    "bun-types": "^1.2.0",
    "typescript": "^5.8.0"
  }
}
